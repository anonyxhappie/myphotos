# MyPhotos – Python dependencies
# Install:  pip install -r requirements.txt

# -- Web framework ---------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# -- Database & ORM --------------------------------------------------------
sqlalchemy>=2.0.30

# -- API schemas -----------------------------------------------------------
pydantic>=2.7.0

# -- Background task queue (SQLite backend) --------------------------------
huey>=2.5.0

# -- Image processing (thumbnails) ----------------------------------------
Pillow>=10.3.0
pillow-heif>=0.16.0             # HEIC/HEIF support

# -- Hashing ---------------------------------------------------------------
imagehash>=4.3.1                # perceptual hashing (pHash)

# -- AI / ML (Phase 3 — install later for a lighter Phase 1) ---------------
lancedb>=0.6.0
open-clip-torch>=2.24.0
torch>=2.2.0
facenet-pytorch>=2.5.3
scikit-learn>=1.4.0
pytesseract>=0.3.10

# -- Dev / Testing ---------------------------------------------------------
pytest>=8.0.0
httpx>=0.27.0                   # for TestClient
watchdog>=4.0.0
